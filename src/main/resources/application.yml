spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/book_reservation_system
    username: root
    password: admin

mybatis:
  configuration:
    map-underscore-to-camel-case: true

server:
  servlet:
    context-path: /api/v1
